---
const { imgSrc, nombre, descripcion, year } = Astro.props;
---

<div class="bg-[#161616] rounded-xl shadow-md p-6 max-w-sm w-full flex flex-col gap-4 border border-[#1a1a1a]">
  <!-- Header fijo -->
  <div class="flex items-start justify-between gap-3 min-h-[56px]">
    <div class="flex items-center gap-3 min-w-0">
      <img 
        src={imgSrc}
        alt={nombre}
        class="w-12 h-12 rounded-full object-cover border border-[#232323]"
        loading="lazy"
      />
      <span class="text-lg font-bold text-white font-poppins break-words leading-tight">
        {nombre}
      </span>
    </div>
    <span class="text-secondary font-semibold text-base ml-2 flex-shrink-0">{year}</span>
  </div>
  <!-- Descripción siempre alineada igual -->
  <div class="flex-1 flex flex-col justify-start">
    <p class="text-[#e0e0e0] text-base leading-relaxed font-poppins">
      {descripcion}
