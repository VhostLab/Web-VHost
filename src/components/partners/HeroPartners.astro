---
// src/components/HeroPartners.astro
---

<section class="bg-[#0b0b0b] text-white relative">
    <!-- Fondo degradado -->
    <div class="absolute inset-0 bg-gradient-to-b from-[#121212] via-[#101010] to-[#0b0b0b] opacity-90 -z-10"></div>
  
    <!-- Contenido -->
    <div class="px-6 py-28">
      <div class="max-w-7xl mx-auto grid grid-cols-1 md:grid-cols-2 items-center gap-16">
        <!-- Texto -->
        <div class="text-center md:text-left">
          <h1 class="text-4xl md:text-5xl font-extrabold font-poppins mb-4">
            ¡Hazte Partner de Vhost!
          </h1>
          <p class="text-lg md:text-xl text-white leading-relaxed font-poppins mb-8 max-w-xl">
            Buscamos creadores, clubs y comunidades. Colabora, gana visibilidad y obtén comisiones por cada cliente que traigas.
          </p>
  
          <!-- Botón + insignia -->
          <div class="flex items-center justify-center md:justify-start gap-4">
            <a
              href="#support-options"
              aria-label="Ir al formulario de partners"
              class="bg-secondary text-black font-extrabold px-10 py-4 rounded-lg hover:bg-accent transition shadow-md hover:shadow-lg"
            >
              ¡QUIERO SER PARTNER!
            </a>
  
            <div class="flex items-center gap-2">
              <i class="fas fa-bolt text-yellow-400 text-lg"></i>
              <span class="text-white font-bold text-base font-poppins">Colabora y gana dinero</span>
            </div>
          </div>
        </div>
  
        <!-- Imagen -->
        <div class="flex justify-center md:justify-end">
          <img
            src="/images/partners/partners.webp"
            alt="Partners Vhost"
            class="w-72 md:w-[22rem] object-contain pointer-events-none animate-bounce-slow"
          />
        </div>
      </div>
    </div>
</section>
  
<style>
  @keyframes bounce-slow {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-6px); }
  }
  .animate-bounce-slow {
    animation: bounce-slow 3s infinite;
  }
</style>
