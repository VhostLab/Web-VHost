---
export interface Feature {
  icon: string;
  name: string;
}
export interface Props {
  title: string;
  price: number;
  desc: string;
  features: Feature[];
}
const { title, price, desc, features } = Astro.props as Props;
---

<div class="group relative">
  <div
    class="absolute -inset-0.5 bg-gradient-to-r from-amber-500 to-pink-500 rounded-xl blur
           opacity-60 group-hover:opacity-100 transition duration-1000 group-hover:duration-200
           animate-tilt"
  ></div>
  <div class="relative rounded-xl bg-black/90 p-8 flex flex-col h-full">
    <h3
      class="text-2xl font-bold text-transparent bg-clip-text
             bg-gradient-to-r from-amber-500 to-pink-500 mb-4"
    >
      {title}
    </h3>
    <p class="text-gray-400 mb-6">{desc}</p>
    <div class="mt-auto">
      <span class="text-4xl font-extrabold text-white">{price.toFixed(2)}</span>
      <span class="text-gray-400">/month</span>
    </div>
    <ul class="mt-6 space-y-4 flex-grow">
      {features.map(f => (
        <li class="flex items-center text-gray-300">
          <svg
            class="h-5 w-5 text-amber-500"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M5 13l4 4L19 7"
            />
          </svg>
          <span class="ml-3">{f.name}</span>
        </li>
      ))}
    </ul>
    <button
      class="mt-8 w-full bg-secondary py-3 px-6 rounded-lg font-semibold
             transform hover:scale-105 transition-all duration-200 hover:shadow-lg"
    >
      <span class="bg-clip-text bg-gradient-to-r">
        Ver Planes
      </span>
    </button>
  </div>
</div>

<!--
<section class="py-16">
  <div class="container mx-auto px-6">
    <h2 class="text-4xl md:text-5xl font-poppins font-bold text-white text-center mb-4">Nuestros Planes</h2>
    <div class="border-t-2 border-secondary w-24 mx-auto mb-12"></div>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      {planData.map(plan => (
        <PlanCard
          title={plan.title}
          desc={plan.desc}
          price={plan.price}
          features={plan.features}
        />
      ))}
    </div>
  </div>
</section>
-->
